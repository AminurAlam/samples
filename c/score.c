#include <stdbool.h>
#include <stdio.h>
#include <stdlib.h>
#define MAX 10

struct Row {
    char name[16];
    int s1, s2, s3;
};

struct Row table[MAX];
int n = 0;

void add_row(char name[], int s1, int s2, int s3) {
    struct Row new;
    table[n++] = ;
}

int main() {
    return 0;
}
